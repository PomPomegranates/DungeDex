@page "/dungemon/new/{SearchQuery}"
@rendermode InteractiveAuto

<PageTitle>DungéDex : New</PageTitle>

<h3>New Dungémon</h3>

@if (!string.IsNullOrEmpty(SearchQuery))
{
    <DungemonLoader idOrName="@SearchQuery" />
}

@code
{
    [Parameter]
    public string SearchQuery { get; set; } = null!;
}

